import { Roboto, Poppins, Bokor } from 'next/font/google';

export const roboto = Roboto({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-roboto',
});

export const poppins = Poppins({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-poppins',
});

export const bokor = Bokor({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-bokor',
});
